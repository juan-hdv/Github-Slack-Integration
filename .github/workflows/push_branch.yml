name: Push branch

on:
  push:
    branches:
      - 'test-branch'

jobs:
  if_pushed:
    runs-on: ubuntu-latest
    steps:
    - name: Echo messages 
      run: |
        echo The Branch was pushed
        echo "End"
